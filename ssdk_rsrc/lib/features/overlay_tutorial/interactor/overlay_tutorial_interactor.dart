class OverlayTutorialInteractor {
  // In a real app you might fetch tutorial steps or check onboarding status
  Future<void> completeTutorial() async {
    // Simulate saving completion state
    await Future.delayed(const Duration(milliseconds: 100));
  }
}
